import { Layout } from "src/components/Layout";
import { Top } from "src/components/Top";

const calendar = () => {
  return (
    <Layout>
      <Top title="カレンダー" />
      <div>calendarです</div>
    </Layout>
  );
};

export default calendar;
